import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/blocks";
import * as PageGridStories from "./PageGrid.stories.tsx";
import README from "../../../../packages/css/src/grid/README.md?raw";

<Meta of={PageGridStories} />

<Markdown>{README}</Markdown>

<Primary />

<Controls />

### Three columns

<Canvas of={PageGridStories.ThreeColumns} />

### Three images

<Canvas of={PageGridStories.ThreeImages} />

### A middle block

Spanning ten columns, numbers 2 up to 11.

<Canvas of={PageGridStories.MiddleBlock} />

### Without the grid cell component

Paragraphs can be used directly on the grid, as they expose the grid cell props as well.

<Canvas of={PageGridStories.Paragraphs} />
