import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/blocks";
import * as BlockquoteStories from "./Blockquote.stories.tsx";
import README from "../../../../../packages/css/src/blockquote/README.md?raw";

<Meta of={BlockquoteStories} />

<Markdown>{README}</Markdown>

<Primary />

<Controls />

### Omgekeerde kleur

Soms gebruik je een citaat op een donkere achtergrond. Gebruik in dit geval `inverseColor`,
zodat de tekst voldoende contrast heeft met de achtergrondkleur.

<Canvas of={BlockquoteStories.InvertedColor} />
