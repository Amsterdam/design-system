import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/blocks";
import * as UnorderedListStories from "./UnorderedList.stories.tsx";
import README from "../../../../../packages/css/src/unordered-list/README.md?raw";

<Meta of={UnorderedListStories} />

<Markdown>{README}</Markdown>

<Primary />

<Controls />

### No Marker

Voor een lijst zonder marker gebruik je de `no-marker` modifier.
Deze wordt gebruikt voor onder andere de navigatie in de header of footer maar ook voor zoekresultaten.

<Canvas of={UnorderedListStories.NoMarker} />
