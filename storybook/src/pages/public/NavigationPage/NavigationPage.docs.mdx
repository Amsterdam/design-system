{/* @license CC0-1.0 */}

import { Description, Meta, Title } from "@storybook/addon-docs/blocks";

import { PageAnatomy } from "#storybook/_components/PageAnatomy/PageAnatomy";

import { anatomyLabels } from "./anatomyLabels.ts";
import * as NavigationStories from "./NavigationPage.stories.tsx";

<Meta of={NavigationStories} />

<Title />

<Description of={NavigationStories} />

## Anatomy

This schematic shows the default sections for this page type and how the grids and vertical space work.

<PageAnatomy labels={anatomyLabels} of={NavigationStories} />

## Usage guidelines

### When to use

Use this page as the entry point of one subject, where the page itself carries little content and mostly points onward.

### When not to use

Use a [Home Page](/docs/pages-public-home-page--docs) for the entry point of the whole site.

## Variants

### With side navigation

Use a side navigation when the page presents one entry from a long list, and visitors move between those entries without leaving the page.
A [Table of Contents](/docs/components-navigation-table-of-contents--docs) is the navigation for this.
Mark the entry on screen with `aria-current="page"`, and set `defaultExpanded` on the outer list so its groups start open.

Put the entry in a [Grid Subgrid](/docs/components-layout-grid--docs) rather than a Cell.
A Subgrid hands the columns it spans to its own children, so the Cells inside it sit on the columns of the page.
It is a single item in the row of the Grid however many rows of its own it holds, so the navigation sits beside all of it and the entry can be any length.

Give the navigation the first place in the source order, so it precedes the entry for a screen reader and for the Tab key.
It only sits beside the entry on the wide grid; on narrower screens it spans the full width above it.

Set `main` on the Subgrid rather than on the Grid around it.
The navigation switches between entries and is a repeated block, so it does not belong to the main content area.
This puts the page title outside the landmark, which is the trade-off for keeping the navigation out of it.

Because the entry’s parts each sit in their own Cell of the Subgrid, the space between them is the row gap.
Leave the row gap of the Subgrid at the vertical gap of the Grid.
That gap has no medium size, so the space below the entry’s heading is larger than the vertical space guide advises for a heading.
Keep elements in one cell where you need the exact space between them.

A map belongs on this page as an interactive component, not as an [Image](/docs/components-media-image--docs).

## Structure

- Set a size of ‘level-3’ on the headings in Link Sections (blocks consisting of a Heading, Paragraph, Standalone Link and/or a Link List).
  Card Headings already set this by default.
- If multiple Cards and Link Sections are grouped, update their Heading levels accordingly.

## Layout and spacing

- Grids on this page type use the default x-large vertical gap.

## Accessibility

A page with a side navigation has two places worth reaching, so give it a second [Skip Link](/docs/components-navigation-skip-link--docs): one to the navigation and one to the entry.
Name each after the thing it reaches rather than numbering them.

A map needs a text alternative, whether it is an image or an interactive component.
Here the list of addresses carries everything the map shows, which is why the placeholder takes an empty `alt`.

Give a repeated action such as ‘Toon op de kaart’ a [visually hidden](/docs/utilities-css-visually-hidden--docs) suffix naming what it acts on.
Without it, every [Button](/docs/components-buttons-button--docs) reads the same in a list of buttons.
Put the suffix after the visible label, so that label stays a contiguous part of the accessible name and speech input still reaches the button.

## See also

- [Home Page](/docs/pages-public-home-page--docs) – the same job for the whole site.
- [Table of Contents](/docs/components-navigation-table-of-contents--docs) – the side navigation.
- [Grid](/docs/components-layout-grid--docs) – the Subgrid the entry sits in.
