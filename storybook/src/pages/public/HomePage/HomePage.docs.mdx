{/* @license CC0-1.0 */}

import { Description, Meta, Title } from "@storybook/addon-docs/blocks";

import { PageAnatomy } from "#storybook/_components/PageAnatomy/PageAnatomy";

import { anatomyLabels } from "./anatomyLabels.ts";
import * as HomeStories from "./HomePage.stories.tsx";

<Meta of={HomeStories} />

<Title />

<Description of={HomeStories} />

## Anatomy

This schematic shows the default sections for this page type and how the grids and vertical space work.

<PageAnatomy labels={anatomyLabels} of={HomeStories} />

## Usage guidelines

### When to use

Use this page as the entry point of a website, where a visitor arrives without a subject in mind.

### When not to use

Use a [Navigation Page](/docs/pages-public-navigation-page--docs) for the entry point of one subject within the site.

## Structure

The page opens with an [Overlap](/docs/components-layout-overlap--docs) rather than a breadcrumb, stacking a [Search Field](/docs/components-forms-search-field--docs) on a background [Image](/docs/components-media-image--docs).
An Overlap runs to the full width of the page, so it is a sibling of the Grids rather than a child of a Cell.
Everything below it sits on a Grid again.
See [Page structure](/docs/pages-guidelines-page-structure--docs) for the shell around it.

A home page has no single most prominent subject, so it usually has no visible level 1 [Heading](/docs/components-text-heading--docs).

- Add a visually hidden level 1 heading with a text like ‘Homepage van (name of the organisation)’.
  Assign level 2 and a size of ‘level-1’ to the visible headings.
- Use the default size of ‘level-3’ headings in Cards and set their appropriate levels.
- Do the same for blocks consisting of Heading, Paragraph, Standalone Link and Link List.

## Layout and spacing

- In Grids of Cards, use a large vertical gap.

## See also

- [Navigation Page](/docs/pages-public-navigation-page--docs) – the same job for a single subject.
- [Overlap](/docs/components-layout-overlap--docs) – the full-width opening section.
- [Page structure](/docs/pages-guidelines-page-structure--docs) – the shell every page shares.
