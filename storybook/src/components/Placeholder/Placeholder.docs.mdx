{/* @license CC0-1.0 */}

import { Canvas, Controls, Description, Meta, Primary, Title } from "@storybook/addon-docs/blocks";

import { DesignTokensTable } from "#storybook/_components/DesignTokensTable/DesignTokensTable";
import tokens from "#tokens/components/ams/placeholder.tokens.json";

import * as PlaceholderStories from "./Placeholder.stories.tsx";

<Meta of={PlaceholderStories} />

<Title />

<Description of={PlaceholderStories} />

<Primary />

<Controls />

## Usage guidelines

### When to use

Show a Placeholder while loading content that takes a noticeable time to arrive – roughly between 1 and 10 seconds.
Use it when you know the shape of the expected content, so the grey blocks can approximate it.
This makes waiting feel shorter and prepares users for the page structure.

### When not to use

Do not show a Placeholder for waits shorter than about a second: a flash of grey blocks is more distracting than helpful.
For a process with a known duration or progress, such as a file upload, show actual progress information instead.
Never use a Placeholder for content that is already available – render that content immediately.
Avoid combining it with other loading indicators in the same view.

### How to use

Choose the appearance that matches the component the Placeholder stands in for: a Card, a Heading of level 2, an Unordered List, a Paragraph, or a Table.
Set `lines`, `rows`, and `columns` to approximate the amount of content you expect.
Render the Placeholder in the same container as the content that will replace it, so both are equally wide.

The Placeholder itself is hidden from assistive technologies.
You must announce the loading state in another way to meet [WCAG 4.1.3](https://www.w3.org/TR/WCAG22/#status-messages).
Provide a status message outside the loading region and mark that region with `aria-busy`:

```html
<div class="ams-visually-hidden" role="status">De zoekresultaten worden geladen</div>
<section aria-busy="true">
  <!-- Placeholders -->
</section>
```

When the content has loaded, set `aria-busy` to `false` and update or clear the status message.

## Examples

### Card

Stands in for a Card, with an image, a heading, and a few lines of text.

<Canvas of={PlaceholderStories.Card} />

### Heading

Stands in for a Heading, sized to a level 2 heading.

<Canvas of={PlaceholderStories.Heading} />

### List

Stands in for an Unordered List, with a marker and a shorter line for each item.

<Canvas of={PlaceholderStories.List} />

### Table

Stands in for a Table, with a header row and evenly divided columns.

<Canvas of={PlaceholderStories.Table} />

## Features

The dimensions of the blocks derive from the design tokens of the components they mimic.
Replacing a Placeholder with the real content therefore barely shifts the layout.

Line lengths vary deterministically – for example, the last line of a paragraph is shorter, and list items alternate in length.
This looks natural yet renders identically on every request, which keeps server-side rendering consistent and visual regression tests stable.

## Design

The blocks use a single neutral grey that reads as a quiet backdrop rather than content.
This first version is intentionally static; a subtle loading animation may be added later.

## Accessibility

The component is decorative and renders with `aria-hidden="true"` by default, so screen readers skip the grey blocks.
Announcing the loading state is the responsibility of the application – see ‘How to use’.
In forced colours mode, the blocks remain visible through the system colour for disabled text.

## Design tokens

<DesignTokensTable tokens={tokens} />
