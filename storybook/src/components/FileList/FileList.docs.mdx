{/* @license CC0-1.0 */}

import { Canvas, Controls, Description, Meta, Primary, Source, Title } from "@storybook/addon-docs/blocks";

import { DesignTokensTable } from "#storybook/_components/DesignTokensTable/DesignTokensTable";
import tokens from "#tokens/components/ams/file-list.tokens.json";

import FileInputWithFileList from "./FileInputWithFileList?raw";
import * as FileListStories from "./FileList.stories.tsx";
import * as FileListItemStories from "./FileListItem.stories.tsx";

<Meta of={FileListStories} />

<Title />

<Description of={FileListStories} />

<Primary />

This component has no props to configure.

## Subcomponents

### Item

Renders the `li` element for one file, around whatever is put inside it.
Place a [File Card](/docs/components-forms-file-card--docs) there, which displays the file and its actions.

<Canvas of={FileListItemStories.Item} />

<Controls of={FileListItemStories.Item} />

The `file` and `onDelete` props are deprecated and will be removed on or after 2027-04-01.
They still work, and warn in the console, but they render a File Card without a preview: the thumbnail they used to display came from an object URL that was never released.
Pass `previewUrl` to a File Card of your own to display one again.

```diff
- <FileList.Item file={file} onDelete={remove} />
+ <FileList.Item>
+   <FileCard name={file.name} onDelete={remove} size={file.size} type={file.type} />
+ </FileList.Item>
```

## Usage guidelines

### When to use

Use a File List to display several files as a list, so that screen readers announce how many there are.
A single file needs no list: use a File Card on its own.

## Examples

### Using a file input

To connect a File Input to a File List, use the `onChange` event to update the list of files, and `onDelete` to remove one.
The object URL for a preview is created as the selection changes and released in an effect clean-up, so no address outlives the file it points at.

Removing the last file empties the list, which is the one case a File Card cannot handle for itself.
This example sends focus to a line of its own that says the files are gone.
Sending it back to the field instead is unreliable.
The wording beside the browse button is generated by the browser, and not every browser updates what a screen reader reads there when the selection changes in code.
One that does not will announce the file that has just been removed.

<Canvas of={FileListStories.WithInput} />

<Source code={FileInputWithFileList} language="jsx" />

## Design

A File List is a column of File Cards with even space between them.
Everything about how one file is displayed – the preview column, the name, the details below it – belongs to the File Card and is documented there.

## Accessibility

A File List renders `ul` and `li` elements, so screen readers announce it as a list and report how many files it holds.

Removing a file moves focus to the delete button of the file next to it, which is what keeps someone working through the list with a keyboard from losing their place.
That behaviour lives in the File Card, and its documentation covers the case this component cannot handle: the last file leaving, which takes the list with it.

## See also

- [File Card](/docs/components-forms-file-card--docs) – displays one file, within this list or on its own.
- [File Input](/docs/components-forms-file-input--docs) – lets the user select files to add to the File List.

## Design tokens

<DesignTokensTable tokens={tokens} />
