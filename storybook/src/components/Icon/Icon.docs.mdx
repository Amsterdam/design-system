{/* @license CC0-1.0 */}

import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/blocks";
import * as IconStories from "./Icon.stories.tsx";
import README from "../../../../packages/css/src/components/icon/README.md?raw";
import { StatusBadge } from "../../docs/components/StatusBadge";

<Meta of={IconStories} />

<StatusBadge description="The set of available icon names and the component API will change." />

<Markdown>{README}</Markdown>

## How to use

Use the React Icon component together with a React SVG component from `@aram-limpens/design-system-react-icons`.
Import this SVG as follows:

```tsx
import { EmailIcon } from "@aram-limpens/design-system-react-icons";
```

Then, you can use it in the component like this:

```tsx
<Icon svg={EmailIcon} />
```

<Primary />

<Controls />

## Examples

### With body text

An icon is as large as regular body text, e.g. paragraphs and lists, including its line height.

<Canvas of={IconStories.WithBodyText} />

Use the `size` prop to pair an icon with small or large body text.

<Canvas of={IconStories.WithSmallBodyText} />

<Canvas of={IconStories.WithLargeBodyText} />

### With a heading

Use a `size` of `heading-3` – `heading-6` to pair an icon with a [Heading](/docs/components-text-heading--docs).

<Canvas of={IconStories.WithHeading3} />

<Canvas of={IconStories.WithHeading4} />

<Canvas of={IconStories.WithHeading5} />

<Canvas of={IconStories.WithHeading6} />

### Square

An icon is placed inside a container that has the same line height as text.
This helps align the icon with the text.
It also makes the container rectangular.
Sometimes, it's better for the icon to be square, such as with the [Icon Button](/docs/components-buttons-icon-button--docs).

<Canvas of={IconStories.Square} />

### Inverse colour

Use the inverse colour if the Icon sits on a dark background.
This ensures the colour of the icon provides enough contrast.

<Canvas of={IconStories.InverseColour} />
