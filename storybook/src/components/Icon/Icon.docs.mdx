{/* @license CC0-1.0 */}

import { Canvas, Controls, Description, Meta, Primary, Title } from "@storybook/addon-docs/blocks";

import { DesignTokensTable } from "#storybook/_components/DesignTokensTable/DesignTokensTable";
import tokens from "#tokens/components/ams/icon.tokens.json";

import * as IconStories from "./Icon.stories.tsx";

<Meta of={IconStories} />

<Title />

<Description of={IconStories} />

<Primary />

<Controls />

## Usage guidelines

### When to use

Use an Icon to support textual interactive elements like buttons and links.
Always try to provide accompanying text for an icon.

Always wrap an SVG icon in an Icon component for proper styling and sizing.

Use the React Icon component together with a React SVG component from `@amsterdam/design-system-react-icons`:

```tsx
import { MailIcon } from "@amsterdam/design-system-react-icons";

<Icon svg={MailIcon} />;
```

### When not to use

Only use icons without accompanying text if they adhere to international standards, such as menu (hamburger icon), search (magnifying glass), next, previous, play, pause, etc.

Our icons can only be used on websites where the City of Amsterdam is the main communicator.

## Examples

### Aligning with text

While the icon itself is square, the wrapping Icon component adds a line height for easy alignment with text.
An icon aligns with regular body text by default.
Use the `size` prop to pair an icon with small or large body text or with a heading.

<Canvas of={IconStories.WithBodyText} />

<Canvas of={IconStories.WithAHeading} />

### Square

An icon is placed inside a container that has the same line height as text.
This helps align the icon with the text.
It also makes the container rectangular.
Sometimes, it is better for the icon to be square, such as with the [Icon Button](/docs/components-buttons-icon-button--docs).

<Canvas of={IconStories.Square} />

### Inverse colour

Use the inverse colour if the Icon sits on a dark background.
This ensures the colour of the icon provides enough contrast.

<Canvas of={IconStories.InverseColour} />

### Right-to-left

Some icons imply a reading direction or movement, such as arrows and vehicles.
These mirror automatically when used inside a right-to-left (RTL) context.
Most icons, such as universal symbols and media controls, are unaffected by direction and remain unchanged.

<Canvas of={IconStories.RightToLeft} />

## Features

Icons have extra whitespace at their top and bottom to match the line height of text components.
Specify the appropriate size to pair an icon with small or large body text or with a heading.
[Read more in the typography documentation](/docs/brand-design-tokens-typography--docs).

Icons are aligned to the left of the text by default and vertically centred to the middle of the first line of text.

The icon defaults to the colour of the container it is in.

## Design

Icons are black or white.
When used as part of an interactive element, they are blue.
When disabled, they are grey.
They must adhere to the same contrast ratio for accessibility as typography against a background colour.

An Icon has no colour of its own: it takes the colour of the text around it, and its artwork is filled with that same colour.
This is why an Icon inside a Button or a Link changes colour along with its label on hover, without either component saying anything about the icon.

The artwork is drawn at one em of the container’s font size, and that font size comes from a token for each option of the `size` prop.
Every option matches a step in the type scale, so choosing a size pairs an icon with a text style rather than with a measurement.
Those tokens are set in rem, so an icon grows along with the font size a user has chosen.

The `square` variant widens that container to match its height.
An icon then sits in the middle of a square instead of a rectangle, which is what a surrounding button needs to end up square itself.

## Accessibility

The markup an Icon renders carries the `hidden` attribute, and the stylesheet is what reveals it again.
If the stylesheet fails to load, the icon is absent rather than dropped into the page at its intrinsic size, and nothing about it reaches assistive technology.

An Icon has no accessible name of its own and adds nothing to the name of whatever contains it.
It is decoration for text that carries the meaning, which is why ‘When to use’ asks for accompanying text and ‘When not to use’ limits the exceptions to symbols everyone already knows.

## See also

- [Icon Button](/docs/components-buttons-icon-button--docs) – a button that consists of a single icon.
- [Icons overview](/docs/brand-assets-icons--docs) – the full set of available icons.

## Design tokens

<DesignTokensTable tokens={tokens} />
