{/* @license CC0-1.0 */}

import { Canvas, Controls, Description, Meta, Primary, Title } from "@storybook/addon-docs/blocks";

import * as TimeInputStories from "./TimeInput.stories.tsx";
import tokens from "../../../../packages-proprietary/tokens/src/components/ams/time-input.tokens.json";

import { DesignTokensTable } from "../../_components/DesignTokensTable/DesignTokensTable";

<Meta of={TimeInputStories} />

<Title />

<Description of={TimeInputStories} />

<Primary />

<Controls />

## Usage guidelines

### When to use

Use a Time Input when the user must enter a time of day.

### When not to use

## Examples

### Invalid

<Canvas of={TimeInputStories.Invalid} />

### Disabled

<Canvas of={TimeInputStories.Disabled} />

### In a Field

Use a Field to group a Time Input with a Label, description and / or an Error Message.

If you don’t need the description, remove its Paragraph and the `aria-describedby` from the Time Input.

Check [the Field docs](/docs/components-forms-field--docs) for more information on configuring it.

<Canvas of={TimeInputStories.InAField} />

### In a Field with validation

If the Time Input can become invalid, add an Error Message and its `id` to the `aria-describedby` attribute of the Time Input.

Check [the Field docs](/docs/components-forms-field--docs) for more information on configuring it.

<Canvas of={TimeInputStories.InAFieldWithValidation} />

## Features

## Design choices

This component uses a native time input, which is styled differently in different browsers and operating systems.
Recreating the native functionality is quite difficult and prone to accessibility errors, which is why we’ve chosen not to do that.
This does mean that this component does not look identical on all browsers and operating systems.

## Accessibility

Wrap a Time Input in a Field to associate it with a Label, description, and Error Message.

## Related components

- [Date Input](/docs/components-forms-date-input--docs) – the counterpart for entering a date.
- [Field](/docs/components-forms-field--docs) – wraps a Time Input with its Label, description, and Error Message.

## Design tokens

<DesignTokensTable tokens={tokens} />
