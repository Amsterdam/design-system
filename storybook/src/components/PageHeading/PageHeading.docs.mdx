{/* @license CC0-1.0 */}

import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/addon-docs/blocks";
import * as PageHeadingStories from "./PageHeading.stories.tsx";
import README from "../../../../packages/css/src/components/page-heading/README.md?raw";
import { StatusBadge } from "../../docs/components/StatusBadge";

<StatusBadge
  description="We no longer use this size of headings. Use the Heading component with level 1 instead."
  status="deprecated"
/>

<Meta of={PageHeadingStories} />

<Markdown>{README}</Markdown>

<Primary />

<Controls />

## Examples

### Inverse colour

Use the inverse colour if the Page Heading sits on a dark background.
This ensures the colour of the text provides enough contrast.

<Canvas of={PageHeadingStories.InverseColour} />
