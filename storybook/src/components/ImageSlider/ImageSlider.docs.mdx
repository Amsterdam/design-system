{/* @license CC0-1.0 */}

import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/blocks";
import * as ImageSliderStories from "./ImageSlider.stories.tsx";
import README from "../../../../packages/css/src/components/image-slider/README.md?raw";

<Meta of={ImageSliderStories} />

<Markdown>{README}</Markdown>

<Primary />

<Controls />

## Examples

### No Controls

<Canvas of={ImageSliderStories.NoControls} />

### No Thumbnails

<Canvas of={ImageSliderStories.NoThumbnails} />

### Snapstop

Setting `snapStop` to false will make the slider not stop at each image.

<Canvas of={ImageSliderStories.NoSnapstop} />

### Native scrollbar

Turning on `nativeScrollbar` will make the slider use the native scrollbar. You might need this if you disable to other controls.

<Canvas of={ImageSliderStories.NativeScrollbar} />

### Various Sizes

When you mix images of different sizes, the slider will adjust to the size of the largest image.

<Canvas of={ImageSliderStories.VariousSizes} />
