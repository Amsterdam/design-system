{/* @license CC0-1.0 */}

import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/blocks";
import * as ImageSliderStories from "./ImageSlider.stories.tsx";
import README from "../../../../packages/css/src/components/image-slider/README.md?raw";
import { StatusBadge } from "../../docs/components/StatusBadge";

<Meta of={ImageSliderStories} />

<StatusBadge reason="Needs work optimizing rendering" />

<Markdown>{README}</Markdown>

<Primary />

<Controls />

## Examples

### No Controls

<Canvas of={ImageSliderStories.NoControls} />

### Source set images

To use source set images, you need to pass an array of objects with `srcSet` and `src` properties. The `srcSet` property should be a string with the image URLs separated by commas. The `src` property should be the URL of the image to use as a fallback.

<Canvas of={ImageSliderStories.SourceSetImages} />
