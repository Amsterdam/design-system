{/* @license CC0-1.0 */}

import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/blocks";
import * as IconButtonStories from "./IconButton.stories.tsx";
import README from "../../../../packages/css/src/components/icon-button/README.md?raw";

<Meta of={IconButtonStories} />

<Markdown>{README}</Markdown>

<Primary />

<Controls />

## Examples

### On a coloured background

An Icon Button on a coloured background must adjust its foreground colour to provide enough contrast.
We have lighter and darker background colours, and Icon Buttons behave differently on each.

- Set the `color` prop to `contrast` on a azure, lime, orange or yellow background.
  It will make the Icon Button black.
- Set the `color` prop to `inverse` on a blue, green, magenta or purple background.
  It will make the Icon Button white.

#### Contrast colour

<Canvas of={IconButtonStories.ContrastColour} />

#### Inverse colour

<Canvas of={IconButtonStories.InverseColour} />
