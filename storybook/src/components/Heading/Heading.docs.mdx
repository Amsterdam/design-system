{/* @license CC0-1.0 */}

import { Canvas, Controls, Description, Meta, Primary, Title } from "@storybook/addon-docs/blocks";

import * as HeadingStories from "./Heading.stories.tsx";
import tokens from "../../../../packages-proprietary/tokens/src/components/ams/heading.tokens.json";

import { DesignTokensTable } from "../../_components/DesignTokensTable/DesignTokensTable";

<Meta of={HeadingStories} />

<Title />

<Description of={HeadingStories} />

<Primary />

<Controls />

## Usage guidelines

### When to use

Headings help the user scan the structure of a page.
They also introduce the content that follows them.

### When not to use

### How to use

Use [Margin](/docs/utilities-css-margin--docs) or [Prose](/docs/utilities-css-prose--docs) to add vertical whitespace.

Use the `size` prop to make a Heading visually larger or smaller, but sparingly and consistently.

The `level` prop has no default; determine the correct level for each instance.
Every page should have one Heading with level 1.
Do not skip levels: a level 2 Heading must be followed by level 3, not level 4.

- Content pages, e.g. for articles, products, events etc. must not use the size prop on their Headings.
- Most Headings on overview pages, e.g. Card, Table of Content, and 'Link Blocks', use a size of 'level-3'.
- The same applies to large sections – e.g. Accordion, Alert, Dialog and the caption of a Table.
  Most of them can be given a different size if appropriate.

## Examples

### Levels

We’ve limited the number of heading levels to four.
More detailed content should probably be split into multiple pages or have some sections merged.

#### Level 1

<Canvas of={HeadingStories.Level1} />

#### Level 2

<Canvas of={HeadingStories.Level2} />

#### Level 3

<Canvas of={HeadingStories.Level3} />

#### Level 4

<Canvas of={HeadingStories.Level4} />

### Sizes

Sometimes, it may be visually more suitable to make a heading appear a bit smaller.
The `size` prop can be used for this.
Do so sparingly; the sizes have been designed to match their semantic level.
Ensure that you still select a level that accurately represents the section’s hierarchy.
These, for example, are all Headings at level 2, but with sizes 1 to 5:

<Canvas of={HeadingStories.Sizes} />

### Inverse colour

Use the inverse colour if the Heading sits on a dark background.
This ensures the colour of the text provides enough contrast.

<Canvas of={HeadingStories.InverseColour} />

### With an icon

Headings can be paired with an icon.
Use a [Row](/docs/components-layout-row--docs) and the `size` prop of [Icon](/docs/components-media-icon--docs) to align them.

<Canvas of={HeadingStories.WithIcon} />

## Features

## Accessibility

Screen reader users navigate by heading to understand page structure.
An incorrect hierarchy disrupts their mental model of the page.

## Related components

- [Prose](/docs/utilities-css-prose--docs) – applies the recommended vertical spacing for editorial content.

## Design tokens

<DesignTokensTable tokens={tokens} />
