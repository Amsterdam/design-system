{/* @license CC0-1.0 */}

import { Canvas, Controls, Markdown, Meta, Primary } from "@storybook/blocks";
import * as BlockquoteStories from "./Blockquote.stories.tsx";
import README from "../../../../packages/css/src/components/blockquote/README.md?raw";

<Meta of={BlockquoteStories} />

<Markdown>{README}</Markdown>

<Primary />

<Controls />

## Examples

### Inverse colour

Set the `inverseColor` prop if the Blockquote sits on a dark background.
This ensures the colour of the text provides enough contrast.

<Canvas of={BlockquoteStories.InvertedColor} />
