/**
 * @license EUPL-1.2+
 * Copyright Gemeente Amsterdam
 */

import type { {{pascalCase name}} } from '@amsterdam/design-system-react/src'
import type { Meta, StoryObj } from '@storybook/react-vite'

import { default as {{camelCase name}}Meta } from './{{pascalCase name}}.stories'

const meta = {
  ...{{camelCase name}}Meta,
  title: '{{titleCase name}}',
} satisfies Meta<typeof {{pascalCase name}}>

export default meta

type Story = StoryObj<typeof meta>

export const Test: Story = {
  // NOTE: When adding new variants, ensure they are included in the test story's render function for visual regression testing.
  tags: ['!dev', '!autodocs'],
}
