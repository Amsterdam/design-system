/**
 * @license EUPL-1.2+
 * Copyright Gemeente Amsterdam
 */

import type { {{ attributes }}, PropsWithChildren } from 'react'

import { clsx } from 'clsx'
import { forwardRef } from 'react'

export type {{pascalCase name}}Props = Readonly<PropsWithChildren<{{ attributes }}<{{ type }}>>>

/**
 * {{description}}
 *
 * @see {@link https://designsystem.amsterdam/?path=/docs/components-TODO-ADD-GROUP-{{kebabCase name}}--docs {{pascalCase name}} docs at Amsterdam Design System}
 */
export const {{pascalCase name}} = forwardRef<{{ type }}, {{pascalCase name}}Props>(
  ({ children, className, ...restProps }, ref) => (
    <{{ tag }} {...restProps} className={clsx('ams-{{kebabCase name}}', className)} ref={ref}>
      {children}
    </{{ tag }}>
  ),
)

{{pascalCase name}}.displayName = '{{pascalCase name}}'
